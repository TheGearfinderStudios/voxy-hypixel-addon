From 490b80bd10a53790f821649d5d8d7d7c07623228 Mon Sep 17 00:00:00 2001
From: Brian <speed.bran@hotmail.com>
Date: Tue, 31 Mar 2026 20:34:54 -0300
Subject: [PATCH] added hypixel ip detection

Removed test logs

implemented hypixel world identifiers

Fixed rendering freeze

implemented HM-API
---
 build.gradle                                  |  9 ++
 .../me/cortex/voxy/client/VoxyClient.java     |  1 +
 .../voxy/client/hypixel/HypixelManager.java   | 85 +++++++++++++++++++
 .../mixin/minecraft/MixinLevelRenderer.java   |  6 +-
 .../me/cortex/voxy/commonImpl/VoxyCommon.java | 12 +++
 .../voxy/commonImpl/WorldIdentifier.java      | 23 +++--
 .../mixin/minecraft/MixinWorld.java           |  2 +-
 7 files changed, 131 insertions(+), 7 deletions(-)
 create mode 100644 src/main/java/me/cortex/voxy/client/hypixel/HypixelManager.java

diff --git a/build.gradle b/build.gradle
index f2dee0ae..dcbbfe80 100644
--- a/build.gradle
+++ b/build.gradle
@@ -59,8 +59,15 @@ repositories {
         filter {
             includeModuleByRegex("[^\\.]+", "nvidium")
         }
     }
+
+    exclusiveContent {
+        forRepository {
+            maven { url "https://maven.azureaaron.net/releases" }
+        }
+        filter { includeGroup "net.azureaaron" }
+    }
 }
 
 
 def gitCommitHash = { ->
@@ -153,8 +160,10 @@ dependencies {
 
     modCompileOnly("maven.modrinth:vivecraft:1.21.9-1.3.2-fabric")
 
     modCompileOnly("maven.modrinth:flashback:rNCr1Rbs")
+
+    include modImplementation("net.azureaaron:hm-api:1.0.2+1.21.11")
 }
 
 
 def targetJavaVersion = 21
diff --git a/src/main/java/me/cortex/voxy/client/VoxyClient.java b/src/main/java/me/cortex/voxy/client/VoxyClient.java
index 972ba772..5ba72316 100644
--- a/src/main/java/me/cortex/voxy/client/VoxyClient.java
+++ b/src/main/java/me/cortex/voxy/client/VoxyClient.java
@@ -65,8 +65,9 @@ public class VoxyClient implements ClientModInitializer {
 
     @Override
     public void onInitializeClient() {
         DebugEntries.init();
+        me.cortex.voxy.client.hypixel.HypixelManager.init();
 
         ClientCommandRegistrationCallback.EVENT.register((dispatcher, registryAccess) -> {
             if (VoxyCommon.isAvailable()) {
                 dispatcher.register(VoxyCommands.register());
diff --git a/src/main/java/me/cortex/voxy/client/hypixel/HypixelManager.java b/src/main/java/me/cortex/voxy/client/hypixel/HypixelManager.java
new file mode 100644
index 00000000..a8374e93
--- /dev/null
+++ b/src/main/java/me/cortex/voxy/client/hypixel/HypixelManager.java
@@ -0,0 +1,85 @@
+package me.cortex.voxy.client.hypixel;
+
+import net.azureaaron.hmapi.events.HypixelPacketEvents;
+import net.azureaaron.hmapi.network.packet.v1.s2c.LocationUpdateS2CPacket;
+import net.fabricmc.fabric.api.client.networking.v1.ClientPlayConnectionEvents;
+import net.minecraft.client.Minecraft;
+import net.minecraft.client.multiplayer.ServerData;
+import me.cortex.voxy.common.Logger;
+import me.cortex.voxy.commonImpl.VoxyCommon;
+import me.cortex.voxy.commonImpl.WorldIdentifier;
+import me.cortex.voxy.client.core.IGetVoxyRenderSystem;
+import java.util.Objects;
+
+public class HypixelManager {
+    private static boolean isHypixel = false;
+    private static String activeSkyblockArea = null;
+
+    public static void init() {
+        ClientPlayConnectionEvents.JOIN.register((handler, sender, client) -> {
+            onJoin(client);
+        });
+        ClientPlayConnectionEvents.DISCONNECT.register((handler, client) -> {
+            isHypixel = false;
+            activeSkyblockArea = null;
+        });
+
+        // Step 2: HM API listeners
+        HypixelPacketEvents.LOCATION_UPDATE.register(packet -> {
+            if (packet instanceof LocationUpdateS2CPacket location) {
+                String prevArea = activeSkyblockArea;
+                activeSkyblockArea = location.mode().orElse(null);
+                String server = location.serverName();
+                
+                if (isHypixel && !Objects.equals(prevArea, activeSkyblockArea)) {
+                    Logger.info("[Voxy] Hypixel Area Change: " + activeSkyblockArea + " (Server: " + server + ")");
+                    // Trigger renderer reload to start Voxy for the new area or after entering one
+                    Minecraft.getInstance().execute(() -> {
+                        var lr = Minecraft.getInstance().levelRenderer;
+                        if (lr instanceof IGetVoxyRenderSystem getter) {
+                            getter.shutdownRenderer();
+                            getter.createRenderer();
+                        }
+                    });
+                }
+            }
+        });
+
+        // Step 4: Iron Curtain / Isolation
+        VoxyCommon.setIdentifierModifier(id -> {
+            if (id == null) return null;
+            if (isHypixel) {
+                // If we are on Hypixel but haven't confirmed a valid area yet, suppress Voxy
+                if (activeSkyblockArea == null) {
+                    return null;
+                }
+                // Return a world identifier with the area suffixed
+                return new WorldIdentifier(id.key, id.biomeSeed, id.dimension, activeSkyblockArea);
+            }
+            return id;
+        });
+    }
+
+    private static void onJoin(Minecraft client) {
+        ServerData serverData = client.getCurrentServer();
+        if (serverData != null && serverData.ip != null) {
+            String ip = serverData.ip.toLowerCase();
+            isHypixel = ip.contains("hypixel.net");
+            activeSkyblockArea = null;// Reset area on join
+            if (isHypixel) {
+                Logger.info("[Voxy] Hypixel Detected. Gating active until Skyblock area is confirmed.");
+            }
+        } else {
+            isHypixel = false;
+            activeSkyblockArea = null;
+        }
+    }
+
+    public static boolean isHypixel() {
+        return isHypixel;
+    }
+
+    public static String getAreaId() {
+        return activeSkyblockArea;
+    }
+}
diff --git a/src/main/java/me/cortex/voxy/client/mixin/minecraft/MixinLevelRenderer.java b/src/main/java/me/cortex/voxy/client/mixin/minecraft/MixinLevelRenderer.java
index c4fac281..3b428798 100644
--- a/src/main/java/me/cortex/voxy/client/mixin/minecraft/MixinLevelRenderer.java
+++ b/src/main/java/me/cortex/voxy/client/mixin/minecraft/MixinLevelRenderer.java
@@ -78,9 +78,13 @@ public abstract class MixinLevelRenderer implements IGetVoxyRenderSystem {
             return;
         }
         WorldEngine world = WorldIdentifier.ofEngine(this.level);
         if (world == null) {
-            Logger.error("Null world selected");
+            if (me.cortex.voxy.client.hypixel.HypixelManager.isHypixel()) {
+                // Silently return during hypixel gating
+            } else {
+                Logger.error("Null world selected");
+            }
             return;
         }
         try {
             this.renderer = new VoxyRenderSystem(world, instance.getServiceManager());
diff --git a/src/main/java/me/cortex/voxy/commonImpl/VoxyCommon.java b/src/main/java/me/cortex/voxy/commonImpl/VoxyCommon.java
index 653f96d1..17d2fee9 100644
--- a/src/main/java/me/cortex/voxy/commonImpl/VoxyCommon.java
+++ b/src/main/java/me/cortex/voxy/commonImpl/VoxyCommon.java
@@ -85,6 +85,18 @@ public class VoxyCommon implements ModInitializer {
     public static boolean isAvailable() {
         return FACTORY != null;
     }
 
+    @FunctionalInterface
+    public interface IdentifierModifier {
+        WorldIdentifier modify(WorldIdentifier id);
+    }
+    private static IdentifierModifier identifierModifier = id -> id;
+    public static void setIdentifierModifier(IdentifierModifier modifier) {
+        identifierModifier = modifier;
+    }
+    public static WorldIdentifier modifyIdentifier(WorldIdentifier id) {
+        return identifierModifier.modify(id);
+    }
+
     public static final boolean IS_MINE_IN_ABYSS = false;
 }
\ No newline at end of file
diff --git a/src/main/java/me/cortex/voxy/commonImpl/WorldIdentifier.java b/src/main/java/me/cortex/voxy/commonImpl/WorldIdentifier.java
index 0ede04fe..a10a2423 100644
--- a/src/main/java/me/cortex/voxy/commonImpl/WorldIdentifier.java
+++ b/src/main/java/me/cortex/voxy/commonImpl/WorldIdentifier.java
@@ -24,20 +24,26 @@ public class WorldIdentifier {
 
     public final ResourceKey<Level> key;
     public final long biomeSeed;
     public final ResourceKey<DimensionType> dimension;//Maybe?
+    public final @Nullable String subId;
     private final transient long hashCode;
     @Nullable transient WeakReference<WorldEngine> cachedEngineObject;
 
     public WorldIdentifier(@NotNull ResourceKey<Level> key, long biomeSeed, @Nullable ResourceKey<DimensionType> dimension) {
+        this(key, biomeSeed, dimension, null);
+    }
+
+    public WorldIdentifier(@NotNull ResourceKey<Level> key, long biomeSeed, @Nullable ResourceKey<DimensionType> dimension, @Nullable String subId) {
         if (key == null) {
             throw new IllegalStateException("Key cannot be null");
         }
         dimension = dimension==null?NULL_DIM_KEY:dimension;
         this.key = key;
         this.biomeSeed = biomeSeed;
         this.dimension = dimension;
-        this.hashCode = mixStafford13(registryKeyHashCode(key))^mixStafford13(registryKeyHashCode(dimension))^mixStafford13(biomeSeed);
+        this.subId = subId;
+        this.hashCode = mixStafford13(registryKeyHashCode(key))^mixStafford13(registryKeyHashCode(dimension))^mixStafford13(biomeSeed)^(subId==null?0:mixStafford13(subId.hashCode()));
     }
 
     @Override
     public int hashCode() {
@@ -49,9 +55,10 @@ public class WorldIdentifier {
         if (obj instanceof WorldIdentifier other) {
             return other.hashCode == this.hashCode &&
                     other.biomeSeed == this.biomeSeed &&
                     equal(other.key, this.key) &&//other.key.equals(this.key) &&
-                    equal(other.dimension, this.dimension)//other.dimension.equals(this.dimension)
+                    equal(other.dimension, this.dimension) &&//other.dimension.equals(this.dimension)
+                    ((other.subId == null && this.subId == null) || (other.subId != null && other.subId.equals(this.subId)))
                     ;
         }
         return false;
     }
@@ -146,9 +153,9 @@ public class WorldIdentifier {
         return getWorldId(this);
     }
 
     public static String getWorldId(WorldIdentifier identifier) {
-        String data = identifier.biomeSeed + identifier.key.toString();
+        String data = identifier.biomeSeed + identifier.key.toString() + (identifier.subId == null ? "" : identifier.subId);
         try {
             return bytesToHex(MessageDigest.getInstance("SHA-256").digest(data.getBytes())).substring(0, 32);
         } catch (
                 NoSuchAlgorithmException e) {
@@ -157,9 +164,9 @@ public class WorldIdentifier {
     }
 
     @Override
     public String toString() {
-        return "WorldIdentifier[" + this.key.identifier().toString() + ", " + this.biomeSeed + ", " + this.dimension.identifier().toString() + ']';
+        return "WorldIdentifier[" + this.key.identifier().toString() + ", " + this.biomeSeed + ", " + this.dimension.identifier().toString() + (this.subId == null ? "" : (", " + this.subId)) + ']';
     }
 
     public static class GsonAdapter extends TypeAdapter<WorldIdentifier> {
         public static final GsonAdapter INSTANCE = new GsonAdapter();
@@ -178,8 +185,13 @@ public class WorldIdentifier {
 
             writer.name("dimension");
             writer.value(identifier.dimension.identifier().toString());
 
+            if (identifier.subId != null) {
+                writer.name("subId");
+                writer.value(identifier.subId);
+            }
+
             writer.endObject();
         }
 
 
@@ -190,11 +202,12 @@ public class WorldIdentifier {
 
             var sKey = obj.getAsJsonPrimitive("key").getAsString();
             long biomeSeed = obj.getAsJsonPrimitive("biomeSeed").getAsLong();
             var sDim = obj.getAsJsonPrimitive("dimension").getAsString();
+            var sSub = obj.has("subId") ? obj.getAsJsonPrimitive("subId").getAsString() : null;
 
             var key = ResourceKey.create(Registries.DIMENSION, Identifier.parse(sKey));
             var dim = ResourceKey.create(Registries.DIMENSION_TYPE, Identifier.parse(sDim));
-            return new WorldIdentifier(key, biomeSeed, dim);
+            return new WorldIdentifier(key, biomeSeed, dim, sSub);
         }
     }
 }
diff --git a/src/main/java/me/cortex/voxy/commonImpl/mixin/minecraft/MixinWorld.java b/src/main/java/me/cortex/voxy/commonImpl/mixin/minecraft/MixinWorld.java
index 42fc4c7f..9d9c8f7b 100644
--- a/src/main/java/me/cortex/voxy/commonImpl/mixin/minecraft/MixinWorld.java
+++ b/src/main/java/me/cortex/voxy/commonImpl/mixin/minecraft/MixinWorld.java
@@ -37,7 +37,7 @@ public class MixinWorld implements IWorldGetIdentifier {
     }
 
     @Override
     public WorldIdentifier voxy$getIdentifier() {
-        return this.identifier;
+        return me.cortex.voxy.commonImpl.VoxyCommon.modifyIdentifier(this.identifier);
     }
 }
-- 
2.51.1.windows.1

